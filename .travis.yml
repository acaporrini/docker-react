language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t acaporrini/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true acaporrini/docker-react npm run test -- --coverage